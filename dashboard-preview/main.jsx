import React from "react";
import ReactDOM from "react-dom/client";
import StoreFlowDashboard from "./storeflow_dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StoreFlowDashboard />
  </React.StrictMode>
);
